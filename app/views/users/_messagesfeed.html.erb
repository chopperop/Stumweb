

<% @messages.each do |message| %>
<tr>

  <td class="micropost">
    <span class="content"><%= wrap(message.body) %></span>
    </br> 
    </br>
    <span class="timestamp">
      Posted <%= time_ago_in_words(message.created_at) %> ago by <%= link_to current_user.username, current_user %>
    </span>
          <%# user = @content.user rescue User.find(@content.user_id) %>
      <% if current_user?(current_user) %>
      <td>
      <%#= link_to "delete", destroy_message_users_path(@message), :method => :delete,
                                       :confirm => "Are you sure you want to delete this message?" %>
    </td>
  <% end %>
     
 <% end %>
