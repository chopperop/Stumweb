<%= form_for [@content, current_user.comments.new] do |f| %>
      <%= render 'shared/error_messages', :object => f.object %>
    <div class="field">
    <%= f.text_area :body %>
    </div>
    <div class="actions">
    <%= f.submit "Submit" %>
    </div>
    <% end %>